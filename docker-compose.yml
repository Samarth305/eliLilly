version: '3.8'

services:
  backend:
    build: ./backend
    container_name: git-storyteller-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    restart: always

  frontend:
    build: ./frontend
    container_name: git-storyteller-frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
    restart: always
